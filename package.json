{
  "scripts": {
    "clean": "rimraf _output",
    "start": "yarn clean && snowpack dev",
    "build": "yarn clean && snowpack build",
    "test": "echo \"This template does not include a test runner by default.\" && exit 1",
    "format": "prettier --write .",
    "lint": "prettier --check ."
  },
  "dependencies": {
    "canvas-confetti": "^1.2.0"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.10.0",
    "@snowpack/plugin-parcel": "^1.3.0",
    "prettier": "^2.0.0",
    "rimraf": "^3.0.2",
    "snowpack": "^2.5.0"
  }
}
