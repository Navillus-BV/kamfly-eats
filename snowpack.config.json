{
  "scripts": {
    "mount:_site": "mount _output --to /",
    "mount:src": "mount site/src --to /_dist_",
    "mount:static": "mount static --to /",
    "run:11ty": "eleventy",
    "run:11ty::watch": "$1 --watch"
  },
  "plugins": ["@snowpack/plugin-parcel"]
}
